Loaded CIFAR-10 dataset using torchvision:
- 50k training images with augmentation (transform_train)
- 10k test images with only resizing & normalization (transform_test)
- download=True ensures dataset is automatically fetched if missing

